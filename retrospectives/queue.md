# Retrospective queue (input for m8-newgame-plus)

## emberwake / M0 (2026-07-11)

### Gate + phase stats
- content gate: GREEN first-try for both specialist phases (systems, content). 0 repair rounds.
- completability: GREEN first-try; 1 benign orphan pattern (one-shot intro via blocked_by_flag excluded from monotone proof) — prover could learn the idiom.
- engine: 428/428 unit tests; 0 defects routed back post-delivery; conductor made 1 micro-edit (per-file evidence lines in run_tests.gd — gate-2 requirement the generated runner missed).
- balance: 1 violation caught (boss wipe 0.230 > 0.15), 1 data-patch round to green (0.090). Sim ran 3300 battles via the game engine; interpreter-vs-design-arithmetic agreement was exact on first contact.
- playtest: static green; rusher PASS (seed 1101, 1184 actions, 23 encounters 16W/7F, boss 23 turns, deterministic byte-identical incl. conductor rerun). Found PT-001 (real engine bug: phantom zero-count stacks + shop gold dupe) -> engine-smith fix + regression test (suite 430/430) -> rusher script repaired in 1 round-trip (2 cursor segments; battle events replayed identically). PT-002 deferred to M1 menu-wright.
- full persona-loop stat: 1 defect found by dynamic play that static analysis + 430 unit tests + 3300 sim battles all missed — personas earn their seat.

### RFC candidates surfaced by specialists (not yet filed)
1. stat-model needs a turn-order/speed role marker (engine bound it via project setting `m8/battle/speed_stat` — data, but engine-local).
2. map schema: npc → service binding (shopkeeper NPCs are flavor-only; services live in the pause menu — merchant NPCs should open the shop).
3. scene-registry: "ending" scene type missing (implemented but unregistered).
4. dialogue barks conditional on flags (village should react to flag.tyrant_down — loremaster craft rule currently unimplementable).

### Skill-edit candidates
- m8-engine-smith: engine contract §7 should require per-file summary lines in run_tests.gd output (the one thing the generated runner missed).
- m8-engine-smith: file-size rule (<300 lines) conflicted with the contract's fixed file list — battle.gd 586, algebra.gd 551. Either the rule allows contract-listed cores to exceed, or the contract should list battle/ and algebra/ as splittable modules.
- m8-menu-wright: menu spellcasting (usable_in: menu spells like mend) has no surfaced scene — pause-menu row list in engine-contract/scene work orders should include a Magic row when any menu-usable spell exists.
- m8-balancer: checkpoint authoring guidance could include "verify spike-kill margin vs. heal threshold" — the boss wipe was a heal-threshold jump, likely a recurring pattern.
- m8-conductor: briefing addenda worked well (systems→content→engine relay); formalize the "BINDING NOTES relay" step in the skill body.

### M0 exit test (fresh session, 2026-07-11): PASSED
- All gates re-ran green cold, zero human edits; smoke/rusher traces and balance report bit-identical to committed evidence.
- Resumability gaps found: (1) smoke-gate evidence records no seed or exact command (rerun matched with seed 1101, but only luck/seed-independence guaranteed it); (2) `m8-balancer/scripts/simulate.py` regenerates `balance.md` wholesale, destroying the balancer's appended patch-log narrative on re-verification runs (restored from git); (3) committed `gate_tests.txt` was stale (428/428 phase-4 era) vs. post-PT-001 suite (430/430) — wrap didn't refresh gate evidence after the playtest-phase engine fix.

### Thin-batteries audit
- No script deleted this cycle; none encoding judgment models have overtaken (all three are gate math / proof / harness).

### Cost notes (approximate, this build)
- 4 specialist sub-agents (systems 77k, content 127k, engine-core 281k, scenes 256k tokens) + rusher (pending) + conductor orchestration in main session.

## emberwake / M1 in-progress findings (2026-07-12)
- Character pipeline live: VNCCS 3.0.1 sheet stage runs on Qwen-Image-Edit-2511 (+PoseStudio LoRA, lightning 4-step), NOT Illustrious — research pinned 2509; 2511 required by VNCCS's LoRA. 458s/sheet on one 3090 (fp8, 19.6GB).
- Pose Studio 3D is UI-locked with two server-side bugs (deg→rad double conversion, np.matrix crash); workaround = inject pre-rendered mannequins via pose_data.captured_images. Consider upstreaming fixes or vendoring a renderer at M1 wrap.
- Prompt-scaffold lessons (workflow _meta has full detail): bare "chibi" on the 640x1536 canvas collapses into sticker collages; palette words dye skin; blue screen contaminates, green doesn't; mannequin facing cue too weak → front/back flips.
- Repair-loop candidates for the sheet stage: facing cue on mannequin renders (eye/nose markers), "no shadow on the ground" prompt term, chibi proportions via dedicated stylization stage or license-checked LoRA (NOT base prompt tags).
- SAM3 remains refused (silent-download path disabled in all workflows); Anima remains refused (NC).
- **Human-proposed experiment (2026-07-12): pixel-art-focused Illustrious base for the character pipeline.** Rationale: QIE-2511 preserves input style, so a pixel-art identity reference should propagate through the pose stages; attacks waxy-skin/lanky-proportion defects at the source. Candidates in RESEARCH-M1 §B.3 (Pixo LoRA, Game Character Sprites, Pixel Art Diffusion XL) — each needs an individual Civitai license read + modelVersionId/SHA pin before use. A/B protocol: same Warden battery, both stacks, same gates, side-by-side Tier-3. Biggest expected win: portraits and battle sprites (style survives at that resolution); smallest: 24px overworld frames (downscale dominates).
- Walk-cycle pipeline lessons (agent-validated, 2026-07-12): eye-cue mannequins + facing clause = 4/4 correct facings; facing flips are seed-luck — reseed immediately, prompt escalation made it worse twice; front/back rows need flat-feet mannequins (foot-pitch cues become tiptoe/shoe hallucinations); chroma key must be offline with sole-protected shadow scrub; lean 10-node cycle graph = 33s/direction vs 458s full generator — mint identity once, cycle cheap. Sheet-stage workflow needs its embedded A-pose refs replaced with cue'd mannequins before Ashcaller.
- OPEN: monster art pose scaffold — MakeHuman mannequin path is humanoid-only; quadrupeds/amorphous need pose-free QIE editing or a new scaffold.
- OPEN: shipping sprites need the VNCCS clothing stage (current cycles use the underwear identity base); then cycles re-run with only image2 swapped.
- **M_Pixel LoRA vetted (human-found, 2026-07-12):** civitai model 44960 v3.0 (modelVersionId 52870, sha256 DC9DE8C8BAE8...EA433). License CLEAN — allowCommercialUse {Image,RentCivit,Rent,Sell}, derivatives OK, no credit required. Catch: base model is SD 1.5 — incompatible with every provisioned base (SDXL/Illustrious/Qwen); using it means provisioning an SD1.5 checkpoint (~2GB) as a separate path. Its documented strength is pixel PEOPLE at 512x768 and pixel BUILDINGS/scenes at 768x768 (trigger "pixel", weight 0.5) — best A/B target: the TILESET and building assets, where our SDXL+pixel-art-xl path is untested. Distinct from the Illustrious-pixel character experiment.
- Clothed re-cut lessons (2026-07-12): pinned seeds held across identity swap — 0 generation repairs for 4 directions (identity-ref swap is cheap and stable, the architecture bet paid); post-chain scrub needed 2 revisions for near-black clothing (ink-connectivity boot protection; shadow band raised to 78%) — scrub v2.1 is now the canonical post-chain. Rejected true right profile over mirrored left: rim-light baked inconsistent colors — lighting consistency across views is a sheet-stage prompt candidate. OPEN: near-black outfit contrast vs dark mine tilesets (Tier-4 in-engine differential check when tileset lands).
- Ashcaller chain lessons (2026-07-12): composite-defect root cause found — QIE repaints in place only when mannequin geometry is centered ~30% fill; bottom-anchored/full-height refs trigger mannequin+character composites; identity ref must be rescaled to mannequin scale/position (fix F2, in workflow _meta). Boot-safe prep v2: color-rule scrubs fail per-outfit (red-fleck rule ate brown boots) — outfit-agnostic ink-chain protection is canonical. Cycle stage fully stabilized: 16/16 facings first-try at pinned seeds, 0 repairs. Calibration: ~40 min/character clean (identity 15, clothes 13, cycles 12).
- **SuperPixelArtXL vetted & REFUSED (human-found, 2026-07-12):** civitai 581162 v1.0 (SDXL 1.0 — architecture COMPATIBLE). License FAILS the bar: allowCommercialUse {RentCivit} only (no Image/Sell — can't ship generated images in an MIT game), allowDerivatives False. Mirror of M_Pixel (clean license / wrong arch); this is right arch / locked license. Onboarding filter is two-axis: SDXL-family base AND allowCommercialUse⊇{Image}. Not downloaded.
- **Monster art path decided (2026-07-12): SDXL + pixel-art-xl (the icon path), NOT Illustrious** — A/B on rime_whelp; chosen for style consistency with icons and cleaner pixel silhouettes on non-humanoid subjects. Method: text-prompt generation (no pose scaffold needed — the humanoid mannequin path doesn't apply), 1024 gen → deterministic post-chain → 128/224 transparent PNG. All 4 gate-green single components. Boss (ash_tyrant) at 224 reads convincingly larger than 128 regulars — composition (crouched golem, lava core) carried it, not just size.
- **PROCESS GAP (2026-07-12): agent ran out of credits mid-finalize** — assets were generated and gate-green but not installed/manifested; conductor recovered from scratchpad. Exact per-monster winning seeds not cleanly logged (finals were touched up past raw candidates). Fix: atelier agents should write the manifest entry immediately after each asset passes its gate, not batch at the end — an interrupted run then leaves complete provenance. Candidate for m8-atelier SKILL.md edit.
- OPEN: re-derive clean seeds for the 3 touched-up monsters (re-run candidate range, hash-match) OR regenerate at a pinned seed for full reproducibility before M1 exit.
- Tileset lessons (2026-07-12): "seamless tileable" prompt alone leaves edge seams — solved deterministically in post (quadrant-offset blend: roll by H/2,W/2 so outer ring is provably continuous, center from original, ramp-blended). Model value was in the post-chain not the base. Entity markers (16px) are where SDXL+pixel-art-xl is weakest — 1024→16 downsample muddies fine structure; the queued M_Pixel/pixel-scene A/B should target 16px markers + floor/wall shape read, NOT terrain (already solved). cave_wall needed reseed (too close to cave_floor luma — walkable/blocked read is a gate-able contrast metric worth adding). portal marker weakest at 16px.
- OPEN (gate idea): adjacent-tile luma-contrast check (walkable vs blocked must differ by a margin) — would have caught cave_wall v1 automatically.
- Icon batch lessons (2026-07-12): pixel-art-xl ignores "diagonal" for thin blades (straight swords render too thin to clear 0.15 fill floor) — broad shapes (falchion) render diagonally on their own; thin weapons need subject reshaping not rotation. Dark-subject keying (adaptive border-flood icon_post_chain.py) essential for soot-dark items (ember key, breastplate) — the fixed-tol warm_draught chain would have failed them. Per-icon margin tuning (1-3) part of the mature chain. Weakest: ashwood_stave (wood renders tan not pale-ash — color-word adherence weak for minor material cues).
- **CONDUCTOR PROCESS ERROR (2026-07-12): double-launched agents on one task.** Misread a "still running 27/32" task-notification as agent death and launched a second agent to finish the icons while the first was alive. No corruption occurred ONLY because the first agent detected the live concurrent worker (via process + scratch-file activity, NOT manifest mtime which false-positived), stopped writing shared files, cleaned its orphans, and monitored to completion. Lessons: (1) a task-notification with a partial-progress body is NOT proof of death — verify before relaunching; (2) the graceful-backoff behavior the first agent showed is the model for multi-agent safety and should be encoded in atelier SKILL.md (detect live workers via process/scratch activity, back off shared writes); (3) a concurrent commit swept an uncommitted additive script edit (--rotate) into the pinned-script stream, creating a sha mismatch — reconciled by conductor after verifying byte-identical reproduction (committed d468b456 reproduces installed icons exactly). Fix applied: 8 icon entries re-pinned to the committed script.
